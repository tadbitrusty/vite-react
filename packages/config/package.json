{
  "name": "@resume-vita/config",
  "version": "1.0.0",
  "private": true,
  "description": "Shared configuration for Resume Vita",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "eslint src --ext .ts,.tsx",
    "clean": "rm -rf dist .turbo"
  },
  "devDependencies": {
    "typescript": "^5.4.2"
  }
}